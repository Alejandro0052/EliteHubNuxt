---
scope: Elite Hub MVP -- full PRD scope (prd-Elite_Hub-2026-07-19)
purpose: build-substrate: for BMAD agents and the solo developer only, no external audience
altitude: feature (keeps epics)
updated: 2026-07-22T22:35
---

- (version) Nuxt ^4.0.0, Vue ^3.5.17, Prisma ^6.12.0, TypeScript ^5.8.3 strict, Tailwind CSS 4, Pinia ^3.0.3, pnpm@10.13.1 (packageManager pin)
- (constraint) [ADOPTED] Auth stack layered, not redundant: @sidebase/nuxt-auth (Nuxt module, globalAppMiddleware gates routes) wraps next-auth v4 NuxtAuthHandler + CredentialsProvider (email+bcrypt) + @next-auth/prisma-adapter, JWT session strategy. All three packages load-bearing, none dead weight
- (constraint) [ADOPTED] Server API convention: Nitro-native REST resource folders + HTTP-method-suffixed files (.get/.post/.put/.delete.ts) under server/api/**, one handler per file, no router/controller/service/repository layer -- handlers call prisma.* directly
- (constraint) [ADOPTED] Shared Prisma client singleton at server/utils/prisma.ts, auto-imported by Nitro as global 'prisma' in ~15 of 17 handlers. 2 offenders (server/api/content/[page].get.ts, .put.ts) instantiate their own PrismaClient -- to be fixed to use the singleton
- (constraint) [ADOPTED] Frontend: exclusively (zero useFetch/useAsyncData usage) -- client-side post-mount data fetching, not SSR-fetched. Pinia only for global auth state (app/stores/auth.ts wrapping #auth useAuth()); everything else local per-page ref/computed. No app/middleware/ directory exists; admin gating duplicated ad hoc across ~10 page components via authStore.user?.isAdmin
- (constraint) [ADOPTED] Error shape: thrown H3 createError({statusCode, message|statusMessage}), no custom envelope. Inconsistent field: some handlers use statusMessage, others message -- needs canonical choice
- (constraint) [ADOPTED] File uploads today: Nitro useStorage('public') driver fs, base ./server/public, written directly to local disk and git-tracked (no gitignore exclusion) -- this is exactly what PRD FR-35 must migrate off of. Filename convention drifts per handler (timestamped vs non-timestamped, silent overwrite risk)
- (constraint) [ADOPTED] Data layer: prisma/schema.prisma sole source, 11 migrations, prisma migrate dev runs on every pnpm dev start (dev-only, no migrate deploy step anywhere -- gap for production release). Seed script at prisma/seed.ts uses shared singleton correctly. RBAC (Rol/Permiso/PermisoRol) schema unused by zero server handlers -- all authz is flat isAdmin boolean
- (constraint) No deployment config exists at all: no Dockerfile, no docker-compose, no vercel.json/platform config, no CI (no .github/), nitro preset defaults to node-server. AUTH_SECRET has an insecure inline fallback ('your-secret-key') if env var unset -- real security gap
- (constraint) Two parallel E2E suites tracked in git: Python/pytest (page-object pattern, being replaced per PRD 9.3) and an unwired Playwright/TS scaffold (no webServer/baseURL configured, nested separate npm project in e2e/test/, zero tests written). Also found: dual lockfiles (pnpm-lock.yaml canonical + stray package-lock.json), a likely-dead composable (useContent().getContent() destructures a {data} envelope the API doesn't return, always undefined)
- (decision) AD-1 paradigm: Transaction Script (thin Nitro route handlers over Prisma, current reality) augmented with shared Authorization Guards in server/utils/ for the PRD's new cross-cutting rules (author-or-admin, active-user filter, review-limit, type-gating) -- not a full service layer, minimum needed to prevent handler-to-handler divergence on these rules
- (decision) AD-2 storage: Cloudflare R2 for all uploaded media (profile photos, catalog item images, publicacion images, evento/noticia images) -- S3-compatible API, 10GB free permanent, zero egress, no inactivity-pause risk. Verified against web (July 2026) rather than asserted from training data. Replaces local-disk Nitro useStorage per FR-35
- (decision) AD-3 charting: Nuxt Charts (nuxtcharts.com) for Reportes/Indicadores -- purpose-built Nuxt module, native Tailwind 4 integration matches existing stack, minimal config for the 'muy agradable de ver' bar. User accepted tradeoff of smaller/newer library vs vue-chartjs's maturity. Verified against web (July 2026)
- (decision) AD-4 auth freshness: shared authorization guard re-checks activo and isAdmin against the DB on every authenticated request (not JWT-trust-only), resolving the existing 2-pattern inconsistency by standardizing on the DB-recheck pattern (content/[page].put.ts's minority approach becomes canonical). Required for FR-36 (admin block) to take effect immediately rather than at next login/token-refresh
- (question) Deployment platform/hosting target: user explicitly deferred this decision (not yet ready to choose). Render was recommended (free tier hosts app+Postgres together, no card, 30-60s cold starts) but not adopted. Goes to spine Deferred section -- revisit before or during the FR-35 storage-migration work, since hosting choice may influence env/config shape
- (decision) AD-5 deactivation cascade: shared Prisma where-filter helper (e.g. activeUserFilter()) in server/utils/, mandatory for every list/feed/catalog/directory endpoint whose results include user-authored content, to enforce FR-40 hiding consistently
- (decision) AD-6 frontend guards: introduce app/middleware/admin.ts (Nuxt route middleware, auto-redirect) for admin-only pages, plus a shared composable (e.g. useCanEdit(resource)) for author-or-admin checks reused across Eventos/Noticias, Publicaciones, Resenas, Items de Catalogo -- replaces the ~10 copy-pasted authStore.user?.isAdmin checks
- (decision) AD-7 E2E testing: integrate e2e/test/ Playwright project properly into the root pnpm workspace, configure webServer+baseURL in playwright.config.ts, retire the Python/pytest suite entirely (files + venv) as part of MVP work, matching PRD 9.3
- (constraint) Mechanical cleanups to ratify as Consistency Conventions (not separate ADs, low-controversy fixes to existing drift): (1) all server handlers must use the server/utils/prisma.ts singleton, fix content/[page].get.ts + .put.ts; (2) createError uses 'message' field canonically (matches majority pattern); (3) pnpm is the sole package manager, delete stray package-lock.json; (4) upload filenames follow {resource}-{id}-{timestamp}{ext}, shared extension allowlist in server/utils/, no silent-overwrite keys; (5) AUTH_SECRET must fail fast at startup if unset, no insecure inline fallback; (6) new Prisma models follow existing Spanish PascalCase convention (Publicacion, Resena, ItemCatalogo, MensajeContacto), matching Usuario/Noticia/Evento
- (decision) AD-3 amended: switched from Nuxt Charts to vue-chartjs -- web-verify reviewer found Nuxt Charts' native-Tailwind-integration claim overstated, actually requires adding Nuxt UI v3 as a new dependency, which changes the tradeoff the user originally accepted (minimal footprint). User chose vue-chartjs (Chart.js wrapper, no extra deps beyond Chart.js itself) once informed
- (event) Reviewer gate run: lint_spine.py clean (4 low false-positive placeholders), rubric walker + web-verify + adversarial reviewers dispatched in parallel, findings written to reviews/
- (decision) AD-8 added: Usuario.tipoUsuarioId immutable via Usuario->Informacion->TipoUsuario path, no write path (incl. admin FR-18 override) may change it. Fixes existing bug in server/api/profile/index.put.ts which currently allows changing it, and corrects the spine's ER diagram which wrongly showed a direct Usuario->TipoUsuario edge
- (change) Applied 13 reviewer fixes to spine: action-scoped authorOrAdmin w/ per-resource matrix; requireSession() named as dedicated DB-recheck primitive (AD-4); activeUserFilter parameterized by relation+admin-bypass (AD-5); Resenas explicitly excluded from FR-40 cascade (matches PRD's own deferred gap, not new scope); storage.ts pinned to always return string[]; insert-first-then-upload ordering convention; AD-3 replaced Nuxt Charts with vue-chartjs after user re-decided; named aggregates.ts shared function for SM-4 (excludes deactivated users from both homepage stats and Reportes); useCanEdit split into useResourcePermissions with canEdit/canDelete/canRetract; relation-naming convention extended to 'autor' as canonical; fixed-value-list single-source convention added (flags existing deportistas.vue drift); FR-31 theme persistence pinned to localStorage-only; next-auth <4.23.0 ceiling documented; R2/Workers SDK friction noted near hosting Deferred item. 4 lower-severity adversarial findings consolidated into one Deferred bullet
- (event) Reconciliation pass against PRD: 5 gaps found (FR-33 uncited, privacy stance silent, AD-7 read as one-time not ongoing, infinite-scroll batch size undecided, Cost rationale unstated) -- all fixed directly: FR-33/34 cited in Capability Map with in-scope clarification, field-visibility convention row added, AD-7 reworded to ongoing/non-gating, pagination batch size set to 20/cursor-based, Cost-guardrail rationale line added near Stack table
- (event) spine finalized
